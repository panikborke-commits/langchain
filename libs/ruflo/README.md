# langchain-ruflo

CLI tool for scaffolding and managing LangChain project configurations.

## Usage

```bash
ruflo config init [DIRECTORY] [--name NAME] [--description DESC] [--force]
```

Scaffolds a new LangChain project with `pyproject.toml`, `.env.example`, a
starter `src/` layout, and a `.gitignore`.
